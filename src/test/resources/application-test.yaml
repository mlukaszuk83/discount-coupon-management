spring:
  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

management:
  endpoint:
    health:
      show-components: always
      show-details: never
  health:
    db:
      enabled: true